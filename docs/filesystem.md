# Файловая система начального образа

Обычный режим загружает базовую систему из initramfs в RAM. Сборка создаёт
дерево в `build/rootfs/`, затем упаковывает его в
`out/images/initramfs.cpio.gz`. Для `--system` то же дерево становится
заготовкой отдельного ext4-диска; небольшой `bootstrap.cpio.gz` монтирует
его и передаёт управление корню на диске.
Исходные файлы проекта лежат в `rootfs/`: `/init`, настройки `/etc/` и
короткая команда `neko-help`. Редактировать сгенерированное дерево не нужно.

## Структура

| Путь в NekoOS | Назначение |
| --- | --- |
| `/usr/bin`, `/usr/sbin` | Исполняемые программы и ссылки на applets BusyBox |
| `/usr/bin/cc`, `/usr/bin/tcc` | Компилятор C TinyCC; `cc` — ссылка на него |
| `/usr/include` | Заголовки musl, TinyCC и Linux UAPI |
| `/usr/lib` | musl libc, стартовые объекты и файлы TinyCC |
| `/bin`, `/sbin` | Ссылки на соответствующие папки в `/usr` |
| `/usr/lib64` | Зарезервировано для будущих библиотек |
| `/lib`, `/lib64` | Ссылки на соответствующие папки в `/usr` |
| `/usr/local` | Локальные программы и библиотеки; при запуске с диском сохраняются |
| `/usr/local/etc/neko/services` | Локальные исполняемые скрипты служб; при запуске с диском сохраняются |
| `/etc` | Исходные настройки, имя ОС и учётная запись root |
| `/var/lib` | Сохраняемые данные служб и будущих системных компонентов |
| `/var/lib/neko-services/{enabled,disabled}` | Настройки автозапуска локальных и встроенных служб |
| `/usr/local/lib/neko-pkg/store` | Версии файлов и манифестов установленных пакетов |
| `/usr/local/lib/neko-pkg/active` | Указатели активной версии многофайловых пакетов |
| `/usr/local/share/ИМЯ`, `/usr/local/lib/ИМЯ` | Постоянные пути ресурсов и библиотек NekoPkg/3 |
| `/var/cache`, `/var/log` | Пока остаются в RAM |
| `/var/run`, `/var/lock` | Ссылки на `/run` и `/run/lock` |
| `/home`, `/root` | Домашние папки на виртуальном диске; `/root` доступна только root |
| `/state` | Точка монтирования виртуального диска ext4 при обычном запуске |
| `/tmp`, `/var/tmp` | Права `1777`; `/tmp` всегда временный, `/var/tmp` сохраняется в режиме `--system` |
| `/run` | Временные данные процессов; монтируется как tmpfs |
| `/dev`, `/proc`, `/sys` | Устройства и сведения ядра; монтируются при старте |
| `/mnt`, `/media`, `/opt` | Зарезервированы, пока пустые |

Основной исполняемый файл — `/usr/bin/busybox`; applets (`sh`, `cat`, `ls`
и другие) — ссылки на него. `/usr/sbin/init` тоже ссылается на BusyBox.
Наличие `/bin` и `/sbin` сохраняет привычные пути для скриптов.

При `bash os run` отдельный диск содержит `/root`, `/home`, `/var/lib` и
`/usr/local`. Изменения в остальных каталогах initramfs, например `/etc` или
основной части `/usr`, теряются после выключения. `/tmp` и `/run` получают
отдельные tmpfs. `bash os run --ram` загружает временную систему без диска:
тогда и домашние
папки находятся только в памяти. Команда `neko-service` управляет встроенной
сетевой и локальными службами; изменение автозапуска требует диска.
`neko-pkg` устанавливает команду и дополнительные файлы из проверенного
архива в `/usr/local`, проверяет зависимости и обновляет установленную версию;
управления пользователями ещё нет.
При `bash os run --system` корень `/` — отдельный записываемый ext4-диск
`system.img`; `/etc` и основная часть `/usr` сохраняются. Диск данных
`state.img` подключается вторым и по-прежнему содержит пользовательские пути.
Подробности: [ADR-004](adr/0004-persistent-state.md) и
[ADR-006](adr/0006-boot-services.md), [ADR-010](adr/0010-system-disk.md) и
[ADR-011](adr/0011-system-update.md).

## Контроль образа

`tools/validate_image.py` разбирает настоящий cpio-архив перед запуском VM.
Проверяются обязательные файлы и каталоги, ссылки, тип и номера `/dev/console`
и `/dev/null`, права временных каталогов и `/root`, владелец и фиксированные
временные метки. `bash os test` дополнительно проверяет часть этих путей
изнутри загруженной NekoOS. Подробнее: [ADR-003](adr/0003-rootfs-layout.md).
