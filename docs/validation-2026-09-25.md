# Проверка этапа 3 — 2026-09-25

В Ubuntu/WSL2 из обычной учётной записи `neko` выполнена сборка нового
initramfs. `tools/validate_image.py` разобрал настоящий cpio-архив и проверил
86 элементов: единый `/usr`, типы ссылок, права `/tmp`, `/var/tmp`, `/root`,
владельцев, временные метки и устройства `/dev/console`, `/dev/null`.

`bash os test --no-build` завершился `BOOT_TEST_PASSED`: guest загрузился,
shell восстановился после `exit`, команды прочитали os-release, проверили
каталоги и запись в `/tmp`, вызвали `neko-help` и выключили QEMU штатно.

Интерактивный `bash os run` показал короткий вывод, приглашение `neko#` и
текст `neko-help` на русском. Выход Ctrl+A → X проверен вручную.

Эти проверки относятся к текущей x86_64 VM. Данные гостя сохраняются
только до выключения; управление пакетами и постоянный диск не добавлялись.
